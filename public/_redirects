# Cloudflare Pages: SPA(React Router) 클라이언트 라우팅 지원.
# 정적 파일과 매치되지 않는 모든 경로는 index.html로 폴백시켜
# React Router가 클라이언트에서 라우팅을 처리하도록 한다.
# (이게 없으면 /dashboard, /legal/terms 등을 새로고침/직접 접속 시 404 발생)
/*    /index.html   200
